<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingBottom="16dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/container_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        app:cardCornerRadius="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/back_iv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_theme" />

        <TextView
            android:id="@+id/time_tv"
            style="@style/TextAppearance.Material3.LabelMedium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:layout_marginTop="3dp"
            android:background="@drawable/bg_text_rounded"
            android:paddingHorizontal="16dp"
            android:paddingVertical="4dp"
            android:text="7 минут"
            android:textColor="@color/md_theme_light_primary" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/title_tv"
        style="@style/TextAppearance.Material3.TitleLarge"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/md_theme_light_onSurface"
        app:layout_constraintEnd_toEndOf="@id/container_iv"
        app:layout_constraintStart_toStartOf="@id/container_iv"
        app:layout_constraintTop_toBottomOf="@id/container_iv"
        tools:text="Основы когнитивно - поведенческой психотерапии" />
</androidx.constraintlayout.widget.ConstraintLayout>